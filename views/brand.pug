extends layout

block content
  h1= title 
    - let brand  =[{name:"nike", slogan:"just do it", price: 20},{name:"apple", slogan:"think different" , price: 30},{name:"under armour" , slogan:"the only way is through" , price: 40}]
  div(class="container-fluid")
  div(class="row")


    each novel ,index in results
        div(class="col-sm-2" onclick=`buy(${index})`)  #{brand.brandName} -  #{brand.brandslogan} - #{brand.brandprice}
  
    script.
      function buy(itemNo)
        {
          alert(`You purchased Item ${itemNo+1}`)
        }     